# Configuration Template pour Cardify
# Copiez ce fichier vers backend/.env et frontend/.env et ajustez les valeurs

# =================================
# CONFIGURATION BACKEND
# =================================

# Environnement de développement
NODE_ENV=development

# Port du serveur backend
PORT=5000

# Configuration MongoDB
# Choisissez l'une des deux options ci-dessous :

# Option 1: MongoDB Local (recommandé pour le développement)
MONGODB_URI_LOCAL=mongodb://localhost:27017/cardify

# Option 2: MongoDB Atlas (pour la production)
# MONGODB_URI_ATLAS=mongodb+srv://username:password@cluster.mongodb.net/cardify?retryWrites=true&w=majority

# JWT Secret (OBLIGATOIRE - minimum 32 caractères)
# Générez une clé secrète sécurisée avec: node -p "require('crypto').randomBytes(32).toString('hex')"
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_32_characters_long

# URL du frontend pour CORS
FRONTEND_URL=http://localhost:3000

# =================================
# CONFIGURATION FRONTEND
# =================================

# URL de l'API backend
VITE_API_URL=http://localhost:10000/api

# =================================
# INSTRUCTIONS DE CONFIGURATION
# =================================

# 1. Backend (.env dans le dossier backend/) :
#    NODE_ENV=development
#    PORT=5000
#    MONGODB_URI_LOCAL=mongodb://localhost:27017/cardify
#    JWT_SECRET=votre_cle_secrete_jwt_32_caracteres_minimum
#    FRONTEND_URL=http://localhost:3000

# 2. Frontend (.env dans le dossier frontend/) :
#    VITE_API_URL=http://localhost:10000/api

# 3. Pour MongoDB local :
#    - Installez MongoDB Community Server
#    - Démarrez le service MongoDB
#    - Utilisez MONGODB_URI_LOCAL

# 4. Pour MongoDB Atlas :
#    - Créez un compte sur MongoDB Atlas
#    - Créez un cluster et obtenez l'URI de connexion
#    - Remplacez username, password et cluster dans MONGODB_URI_ATLAS
#    - Commentez MONGODB_URI_LOCAL et décommentez MONGODB_URI_ATLAS

# 5. Générer JWT_SECRET sécurisé :
#    Dans un terminal Node.js, exécutez :
#    require('crypto').randomBytes(32).toString('hex')

# =================================
# COMPTES DE TEST (après seedDatabase)
# =================================

# Admin:
# Email: admin@cardify.com
# Mot de passe: Admin123!

# Business:
# Email: business@cardify.com  
# Mot de passe: Business123!

# Utilisateur:
# Email: user@cardify.com
# Mot de passe: User123!
